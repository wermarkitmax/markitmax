import { a as AppShell, h as fetchActivityLogs, n as CardContent, o as Badge, t as Card } from "./card-DORU6gnJ.js";
import { n as fmtDate } from "./format-C_mDrmAR.js";
import { a as TableHeader, i as TableHead, n as TableBody, o as TableRow, r as TableCell, t as Table } from "./table-BQuBX6bn.js";
import { jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
//#region src/routes/_authenticated/activity.tsx?tsr-split=component
function ActivityPage() {
	const { data = [], isLoading } = useQuery({
		queryKey: ["activity"],
		queryFn: async () => {
			return await fetchActivityLogs();
		}
	});
	return /* @__PURE__ */ jsx(AppShell, {
		title: "Activity",
		children: /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsx(CardContent, {
			className: "p-4",
			children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
				/* @__PURE__ */ jsx(TableHead, { children: "Time" }),
				/* @__PURE__ */ jsx(TableHead, { children: "Action" }),
				/* @__PURE__ */ jsx(TableHead, { children: "Entity" }),
				/* @__PURE__ */ jsx(TableHead, { children: "User" })
			] }) }), /* @__PURE__ */ jsx(TableBody, { children: isLoading ? /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
				colSpan: 4,
				className: "py-10 text-center text-muted-foreground",
				children: "Loading…"
			}) }) : data.length === 0 ? /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
				colSpan: 4,
				className: "py-10 text-center text-muted-foreground",
				children: "No activity recorded yet."
			}) }) : data.map((r) => /* @__PURE__ */ jsxs(TableRow, { children: [
				/* @__PURE__ */ jsx(TableCell, {
					className: "text-xs text-muted-foreground",
					children: fmtDate(r.created_at)
				}),
				/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsx(Badge, {
					variant: "outline",
					children: r.action
				}) }),
				/* @__PURE__ */ jsx(TableCell, { children: r.entity_type ?? "—" }),
				/* @__PURE__ */ jsx(TableCell, {
					className: "font-mono text-xs",
					children: r.user_id?.slice(0, 8) ?? "—"
				})
			] }, r.id)) })] })
		}) })
	});
}
//#endregion
export { ActivityPage as component };
