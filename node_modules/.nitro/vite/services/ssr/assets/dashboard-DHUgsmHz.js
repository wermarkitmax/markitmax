import { t as useAuth } from "./useAuth-CHQfghj6.js";
import { a as AppShell, g as fetchDashboardStats, i as CardTitle, n as CardContent, r as CardHeader, t as Card } from "./card-DORU6gnJ.js";
import { r as fmtNumber, t as fmtCurrency } from "./format-C_mDrmAR.js";
import { jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
import { FileText, FolderKanban, Receipt, TrendingDown, TrendingUp, UserSquare2, Users, Wallet } from "lucide-react";
import { Area, AreaChart, CartesianGrid, Cell, Legend, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
//#region src/routes/_authenticated/dashboard.tsx?tsr-split=component
function Dashboard() {
	const { role } = useAuth();
	const isAdmin = role === "admin";
	const stats = useQuery({
		queryKey: ["dashboard-stats"],
		queryFn: async () => {
			return await fetchDashboardStats();
		}
	});
	const d = stats.data;
	const monthly = (() => {
		const months = [];
		for (let i = 5; i >= 0; i--) {
			const dt = /* @__PURE__ */ new Date();
			dt.setMonth(dt.getMonth() - i);
			const key = dt.toLocaleDateString("en-US", { month: "short" });
			const monthIdx = dt.getMonth();
			const year = dt.getFullYear();
			const rev = (d?.invoicesArr ?? []).filter((iv) => {
				const dd = new Date(iv.issue_date);
				return dd.getMonth() === monthIdx && dd.getFullYear() === year && iv.status === "paid";
			}).reduce((s, i) => s + Number(i.total), 0);
			const exp = (d?.expensesArr ?? []).filter((e) => {
				const dd = new Date(e.expense_date);
				return dd.getMonth() === monthIdx && dd.getFullYear() === year;
			}).reduce((s, e) => s + Number(e.amount), 0);
			months.push({
				name: key,
				revenue: rev,
				expenses: exp
			});
		}
		return months;
	})();
	const adminCards = [
		{
			label: "Revenue",
			value: fmtCurrency(d?.revenue),
			icon: TrendingUp,
			hint: "Paid invoices"
		},
		{
			label: "Expenses",
			value: fmtCurrency(d?.expenses),
			icon: TrendingDown,
			hint: "All time"
		},
		{
			label: "Net Profit",
			value: fmtCurrency(d?.profit),
			icon: Wallet,
			hint: "Revenue − Expenses"
		},
		{
			label: "Outstanding",
			value: fmtCurrency(d?.pending),
			icon: Receipt,
			hint: "Unpaid invoices"
		}
	];
	const opsCards = [
		{
			label: "Leads",
			value: fmtNumber(d?.leadsTotal),
			icon: Users,
			hint: `${d?.leadsWon ?? 0} won`
		},
		{
			label: "Contacts",
			value: fmtNumber(d?.contactsTotal),
			icon: UserSquare2,
			hint: "Active clients"
		},
		{
			label: "Running Projects",
			value: fmtNumber(d?.projectsRunning),
			icon: FolderKanban,
			hint: `${d?.projectsCompleted ?? 0} completed`
		},
		{
			label: "Invoices",
			value: fmtNumber(d?.invoicesArr?.length),
			icon: FileText,
			hint: "Generated"
		}
	];
	const cards = isAdmin ? [...adminCards, ...opsCards] : opsCards;
	const projectPie = [{
		name: "Running",
		value: d?.projectsRunning ?? 0
	}, {
		name: "Completed",
		value: d?.projectsCompleted ?? 0
	}];
	const pieColors = ["var(--accent)", "var(--success)"];
	return /* @__PURE__ */ jsxs(AppShell, {
		title: "Dashboard",
		children: [/* @__PURE__ */ jsx("div", {
			className: "grid gap-4 sm:grid-cols-2 lg:grid-cols-4",
			children: cards.map((c) => /* @__PURE__ */ jsx(Card, {
				className: "border-border",
				children: /* @__PURE__ */ jsx(CardContent, {
					className: "p-5",
					children: /* @__PURE__ */ jsxs("div", {
						className: "flex items-start justify-between",
						children: [/* @__PURE__ */ jsxs("div", { children: [
							/* @__PURE__ */ jsx("div", {
								className: "text-xs uppercase tracking-wider text-muted-foreground",
								children: c.label
							}),
							/* @__PURE__ */ jsx("div", {
								className: "mt-2 font-serif text-3xl",
								children: stats.isLoading ? "—" : c.value
							}),
							/* @__PURE__ */ jsx("div", {
								className: "mt-1 text-xs text-muted-foreground",
								children: c.hint
							})
						] }), /* @__PURE__ */ jsx(c.icon, { className: "h-5 w-5 text-muted-foreground" })]
					})
				})
			}, c.label))
		}), isAdmin && /* @__PURE__ */ jsxs("div", {
			className: "mt-6 grid gap-4 lg:grid-cols-3",
			children: [/* @__PURE__ */ jsxs(Card, {
				className: "lg:col-span-2",
				children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, {
					className: "text-base font-medium",
					children: "Revenue vs Expenses · Last 6 months"
				}) }), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsx("div", {
					className: "h-64",
					children: /* @__PURE__ */ jsx(ResponsiveContainer, { children: /* @__PURE__ */ jsxs(AreaChart, {
						data: monthly,
						children: [
							/* @__PURE__ */ jsxs("defs", { children: [/* @__PURE__ */ jsxs("linearGradient", {
								id: "rev",
								x1: "0",
								y1: "0",
								x2: "0",
								y2: "1",
								children: [/* @__PURE__ */ jsx("stop", {
									offset: "5%",
									stopColor: "var(--accent)",
									stopOpacity: .4
								}), /* @__PURE__ */ jsx("stop", {
									offset: "95%",
									stopColor: "var(--accent)",
									stopOpacity: 0
								})]
							}), /* @__PURE__ */ jsxs("linearGradient", {
								id: "exp",
								x1: "0",
								y1: "0",
								x2: "0",
								y2: "1",
								children: [/* @__PURE__ */ jsx("stop", {
									offset: "5%",
									stopColor: "var(--muted-foreground)",
									stopOpacity: .3
								}), /* @__PURE__ */ jsx("stop", {
									offset: "95%",
									stopColor: "var(--muted-foreground)",
									stopOpacity: 0
								})]
							})] }),
							/* @__PURE__ */ jsx(CartesianGrid, {
								strokeDasharray: "3 3",
								stroke: "var(--border)"
							}),
							/* @__PURE__ */ jsx(XAxis, {
								dataKey: "name",
								stroke: "var(--muted-foreground)",
								fontSize: 12
							}),
							/* @__PURE__ */ jsx(YAxis, {
								stroke: "var(--muted-foreground)",
								fontSize: 12
							}),
							/* @__PURE__ */ jsx(Tooltip, { contentStyle: {
								background: "var(--popover)",
								border: "1px solid var(--border)",
								borderRadius: 8
							} }),
							/* @__PURE__ */ jsx(Area, {
								type: "monotone",
								dataKey: "revenue",
								stroke: "var(--accent)",
								fill: "url(#rev)",
								strokeWidth: 2
							}),
							/* @__PURE__ */ jsx(Area, {
								type: "monotone",
								dataKey: "expenses",
								stroke: "var(--muted-foreground)",
								fill: "url(#exp)",
								strokeWidth: 2
							})
						]
					}) })
				}) })]
			}), /* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, {
				className: "text-base font-medium",
				children: "Project Status"
			}) }), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsx("div", {
				className: "h-64",
				children: /* @__PURE__ */ jsx(ResponsiveContainer, { children: /* @__PURE__ */ jsxs(PieChart, { children: [/* @__PURE__ */ jsx(Pie, {
					data: projectPie,
					dataKey: "value",
					innerRadius: 50,
					outerRadius: 80,
					paddingAngle: 4,
					children: projectPie.map((_, i) => /* @__PURE__ */ jsx(Cell, { fill: pieColors[i] }, i))
				}), /* @__PURE__ */ jsx(Legend, {})] }) })
			}) })] })]
		})]
	});
}
//#endregion
export { Dashboard as component };
