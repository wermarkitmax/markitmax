import { t as useAuth } from "./useAuth-CHQfghj6.js";
import { Navigate, Outlet } from "@tanstack/react-router";
import { jsx } from "react/jsx-runtime";
//#region src/routes/_authenticated.tsx?tsr-split=component
function AuthGate() {
	const { loading, user } = useAuth();
	if (loading) return /* @__PURE__ */ jsx("div", {
		className: "flex min-h-screen items-center justify-center bg-background",
		children: /* @__PURE__ */ jsx("div", { className: "h-8 w-8 animate-spin rounded-full border-2 border-accent border-t-transparent" })
	});
	if (!user) return /* @__PURE__ */ jsx(Navigate, { to: "/auth" });
	return /* @__PURE__ */ jsx(Outlet, {});
}
//#endregion
export { AuthGate as component };
