import { a as AppShell, i as CardTitle, m as fetchAccountsData, n as CardContent, r as CardHeader, t as Card } from "./card-DORU6gnJ.js";
import { t as fmtCurrency } from "./format-C_mDrmAR.js";
import { jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
import { Banknote, PiggyBank, Receipt, TrendingDown, TrendingUp, Wallet } from "lucide-react";
import { Bar, BarChart, CartesianGrid, Cell, Legend, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
//#region src/routes/_authenticated/accounts.tsx?tsr-split=component
function AccountsPage() {
	const { data, isLoading } = useQuery({
		queryKey: ["accounts-data"],
		queryFn: async () => {
			return await fetchAccountsData();
		}
	});
	const revenue = (data?.invoices.filter((i) => i.status === "paid").reduce((s, i) => s + Number(i.total), 0) ?? 0) + (data?.sales.reduce((s, i) => s + Number(i.selling_price) * Number(i.quantity ?? 1), 0) ?? 0);
	const expensesTotal = data?.expenses.reduce((s, e) => s + Number(e.amount), 0) ?? 0;
	const profit = revenue - expensesTotal;
	const margin = revenue > 0 ? Math.round(profit / revenue * 100) : 0;
	const byCategory = Object.entries((data?.expenses ?? []).reduce((acc, e) => {
		const k = e.category ?? "misc";
		acc[k] = (acc[k] ?? 0) + Number(e.amount);
		return acc;
	}, {})).map(([name, value]) => ({
		name,
		value
	}));
	const months = (() => {
		const out = [];
		for (let i = 5; i >= 0; i--) {
			const dt = /* @__PURE__ */ new Date();
			dt.setMonth(dt.getMonth() - i);
			const name = dt.toLocaleDateString("en-US", { month: "short" });
			const m = dt.getMonth();
			const y = dt.getFullYear();
			const rev = (data?.invoices ?? []).filter((iv) => {
				const dd = new Date(iv.issue_date);
				return dd.getMonth() === m && dd.getFullYear() === y && iv.status === "paid";
			}).reduce((s, i) => s + Number(i.total), 0);
			const exp = (data?.expenses ?? []).filter((e) => {
				const dd = new Date(e.expense_date);
				return dd.getMonth() === m && dd.getFullYear() === y;
			}).reduce((s, i) => s + Number(i.amount), 0);
			out.push({
				name,
				revenue: rev,
				expenses: exp
			});
		}
		return out;
	})();
	const cards = [
		{
			label: "Total Revenue",
			value: revenue,
			icon: TrendingUp
		},
		{
			label: "Total Expenses",
			value: expensesTotal,
			icon: TrendingDown
		},
		{
			label: "Net Profit",
			value: profit,
			icon: Wallet
		},
		{
			label: "Profit Margin",
			value: `${margin}%`,
			icon: PiggyBank,
			raw: true
		},
		{
			label: "Avg Invoice",
			value: data?.invoices.length ? Math.round(data.invoices.reduce((s, i) => s + Number(i.total), 0) / data.invoices.length) : 0,
			icon: Receipt
		},
		{
			label: "Sales Count",
			value: data?.sales.length ?? 0,
			icon: Banknote,
			raw: true
		}
	];
	const pieColors = [
		"var(--accent)",
		"var(--chart-2)",
		"var(--chart-3)",
		"var(--chart-4)",
		"var(--chart-5)"
	];
	return /* @__PURE__ */ jsxs(AppShell, {
		title: "Accounts",
		children: [/* @__PURE__ */ jsx("div", {
			className: "grid gap-4 sm:grid-cols-2 lg:grid-cols-3",
			children: cards.map((c) => /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsx(CardContent, {
				className: "p-5",
				children: /* @__PURE__ */ jsxs("div", {
					className: "flex items-start justify-between",
					children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("div", {
						className: "text-xs uppercase tracking-wider text-muted-foreground",
						children: c.label
					}), /* @__PURE__ */ jsx("div", {
						className: "mt-2 font-serif text-3xl",
						children: isLoading ? "—" : c.raw ? c.value : fmtCurrency(Number(c.value))
					})] }), /* @__PURE__ */ jsx(c.icon, { className: "h-5 w-5 text-muted-foreground" })]
				})
			}) }, c.label))
		}), /* @__PURE__ */ jsxs("div", {
			className: "mt-6 grid gap-4 lg:grid-cols-3",
			children: [/* @__PURE__ */ jsxs(Card, {
				className: "lg:col-span-2",
				children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, {
					className: "text-base",
					children: "Monthly Cash Flow"
				}) }), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsx("div", {
					className: "h-72",
					children: /* @__PURE__ */ jsx(ResponsiveContainer, { children: /* @__PURE__ */ jsxs(BarChart, {
						data: months,
						children: [
							/* @__PURE__ */ jsx(CartesianGrid, {
								strokeDasharray: "3 3",
								stroke: "var(--border)"
							}),
							/* @__PURE__ */ jsx(XAxis, {
								dataKey: "name",
								stroke: "var(--muted-foreground)",
								fontSize: 12
							}),
							/* @__PURE__ */ jsx(YAxis, {
								stroke: "var(--muted-foreground)",
								fontSize: 12
							}),
							/* @__PURE__ */ jsx(Tooltip, { contentStyle: {
								background: "var(--popover)",
								border: "1px solid var(--border)",
								borderRadius: 8
							} }),
							/* @__PURE__ */ jsx(Legend, {}),
							/* @__PURE__ */ jsx(Bar, {
								dataKey: "revenue",
								fill: "var(--accent)",
								radius: [
									6,
									6,
									0,
									0
								]
							}),
							/* @__PURE__ */ jsx(Bar, {
								dataKey: "expenses",
								fill: "var(--muted-foreground)",
								radius: [
									6,
									6,
									0,
									0
								]
							})
						]
					}) })
				}) })]
			}), /* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, {
				className: "text-base",
				children: "Expenses by Category"
			}) }), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsx("div", {
				className: "h-72",
				children: byCategory.length === 0 ? /* @__PURE__ */ jsx("div", {
					className: "grid h-full place-items-center text-sm text-muted-foreground",
					children: "No expenses yet"
				}) : /* @__PURE__ */ jsx(ResponsiveContainer, { children: /* @__PURE__ */ jsxs(PieChart, { children: [
					/* @__PURE__ */ jsx(Pie, {
						data: byCategory,
						dataKey: "value",
						nameKey: "name",
						outerRadius: 80,
						children: byCategory.map((_, i) => /* @__PURE__ */ jsx(Cell, { fill: pieColors[i % pieColors.length] }, i))
					}),
					/* @__PURE__ */ jsx(Tooltip, {}),
					/* @__PURE__ */ jsx(Legend, {})
				] }) })
			}) })] })]
		})]
	});
}
//#endregion
export { AccountsPage as component };
